# Superseded model metadata

This directory contains historical calibration metadata that is not read by the
current converter, server, evaluation command, or release workflow.

The only active runtime selection is
`src/buili_plan2bim/models/ACTIVE_RUNTIME.json`. Do not copy values from this
directory into product code or accuracy claims.
